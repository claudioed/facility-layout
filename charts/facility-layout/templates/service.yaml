apiVersion: v1
kind: Service
metadata:
  name: {{ include "facility-layout.fullname" . }}
  labels:
    {{- include "facility-layout.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "facility-layout.selectorLabels" . | nindent 4 }}
